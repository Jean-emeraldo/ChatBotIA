import 'package:flutter/material.dart';

// Bleu ciel / blanc : palette principale de l'application
const Color primaryColor = Color(0xFF38BDF8);
const Color primaryDarkColor = Color(0xFF0284C7);
const Color accentColor = Color(0xFF7DD3FC);
const Color backgroundColor = Color(0xFFF8FBFF);
const Color surfaceColor = Colors.white;
const Color borderColor = Color(0xFFBAE6FD);
const Color textPrimary = Color(0xFF0F172A);
const Color textSecondary = Color(0xFF475569);
const Color textGray = textSecondary;

// 🔠 Style de texte global
const TextStyle titleStyle = TextStyle(
  fontSize: 20,
  fontWeight: FontWeight.bold,
  color: textPrimary,
);

// 📐 Marges / Padding standards
const double defaultPadding = 16.0;
